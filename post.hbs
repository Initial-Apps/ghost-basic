{{!< default}}

{{!-- Everything inside the #post tags pulls data from the post --}}
{{#post}}

<main id="site-main" class="container">
    <div class="inner mt-5 pt-5">
        <article class="{{post_class}} {{#unless feature_image}}no-image{{/unless}}">
            <div class="container">
                <h1>{{title}}</h1>
                <section>
                    <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="D MMMM YYYY"}}</time>
                    {{#primary_tag}}
                        <span>|</span> <a href="{{url}}">{{name}}</a>
                    {{/primary_tag}}
                </section>
            </div>
            {{!-- Displays the featured image if exists --}}
            {{#if feature_image}}
            <figure>
                <img src="{{img_url feature_image}}" alt="{{title}}" class="img-fluid py-4"/>
            </figure>
            {{/if}}
            <div class="container mb-5 pb-5">
                <section>
                    <div class="post-content fs-5">
                        {{!-- Display the post content --}}
                        {{content}}
                    </div>
                    {{!-- Comment section will be displayed here --}}
                    {{!--> "comment"--}}
                </section>                
            </div>
        </article>
    </div>
</main>

{{/post}}